/* ============================================================
   STARMAKER · CONFIG
   Paste your two Supabase values between the quotes below.
   (Find them in Supabase → Project Settings → API.)
   The "anon public" key is SAFE to expose in browser code.
   Until you fill these in, wishes are saved only in your own
   browser (handy for previewing) and are NOT shared.
   ============================================================ */
window.STARMAKER_CFG = {
  url: "https://ryjxzquacowxlmfdmccy.supabase.co",   // ← your Project URL
  key: "sb_publishable_sfrulgu4ATFTTZoU_TPnug_chjDoQBM"                // ← your anon public key
};